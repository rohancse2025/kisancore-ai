# AI Models

Placeholder for:
- training notebooks/scripts
- model registry artifacts
- inference service code (if separate from `backend/`)

Suggested structure:
- `training/`
- `inference/`
- `artifacts/` (kept out of git if large)

